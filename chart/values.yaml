appName: lid
namespace: lulo

image:
  repository: docker.yesidlopez.de/lid
  pullPolicy: IfNotPresent
  tag: "1.4"

service:
  type: LoadBalancer
  port: 3000

# Add resource limits
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
