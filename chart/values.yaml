appName: deu
namespace: lulo

image:
  repository: docker.yesidlopez.de/deu
  pullPolicy: IfNotPresent
  tag: "3.4"

service:
  type: LoadBalancer
  port: 3000

# Add resource limits
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
